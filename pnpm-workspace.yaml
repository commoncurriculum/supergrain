packages:
  - "packages/*"

# pnpm 10 blocks dependency postinstall scripts by default. Playwright's
# postinstall downloads its browsers; without this approval a fresh clone has no
# browsers and every browser-mode test suite silently can't launch.
onlyBuiltDependencies:
  - playwright
